# Book Feedback — Julia ML Competitor

Drop notes/requests here and the daily editor run will treat them as top priority.
Format: one bullet per request. Mark done items with `[x]`.

- Reading-question style (FUTURE questions only — leave 1.3.2 and 1.4.2 as-is, already answered): prefer code-first drills over CS-theory essay prompts. Recast the single-dispatch idea as "write the four `collide` methods for Asteroid/Ship, then show which method `collide(Ship(), Asteroid())` selects and why" instead of "why can single dispatch only fake collide(a,b)". Keep the *concept* (multiple dispatch is the point of the module) but make him type Julia and watch dispatch pick.
- [x] Wording trap to avoid: don't sit a "destructure" question right next to the destructive-`!` material without flagging that destructuring is NON-mutating. Gabriel reasonably read "destructure" as "destructive". When introducing tuple unpacking, state explicitly: `lo, hi = bounds(v)` binds names, leaves the (immutable) tuple untouched — opposite of `!`. (Done 2026-06-16: §1.4 Note "Destructuring is not destructive" + red/green contrast card targeting his `minimum!/maximum!` mistake.)

- LA source / framing (UPDATED 2026-06-16: Gabriel is ALSO supplementing with David C. Lay, "Linear Algebra and Its Applications" — keep M2/M5 vocab compatible with BOTH. They agree on Ax=b as a linear combination of A's columns (Lay's "vector equation"/"matrix equation", Ch 1), so the column picture still leads. Lay adds: row reduction/echelon forms, pivot positions, free vs. basic variables, rank/subspaces (Ch 1–2), and normal-equations least squares + projection (Ch 6). Gloss a term in both vocabularies when they diverge. THE LOCK still holds — neither source is required material.): Align M2 and M5 to Strang's vocabulary and sequencing — lead with the **column picture** of Ax=b (solution = linear combination of A's columns), row vs. column picture, four fundamental subspaces, elimination→LU, projection/least squares. Honor THE LOCK: teach it all in the book, never make the lectures required material. NOTE: do not conflate Julia's column-MAJOR storage (a Fortran/LAPACK/BLAS memory-layout inheritance) with Strang's column PICTURE — unrelated; they only rhyme.

- [x] NEW LESSON REQUEST (from Gabriel, for next run — high priority): explain the `->` operator using the exact line from the M1 exam boss, `σ = sqrt(sum(x -> abs2(x - μ), v) / n)`. Teach it in two layers: (1) `->` builds an **anonymous function** — `x -> abs2(x - μ)` is a one-argument function with no name that *captures* `μ` from the enclosing scope (a closure); contrast with the named `f(x) = abs2(x - μ)`. (2) The higher-order form `sum(f, v)` applies `f` to each element and reduces in **one pass with zero intermediate array** — tie it back to why the fast normalizer beat the slow one (no `[c^2 for c in …]` temporary). Typeset the variance/σ math in LaTeX: \(\sigma = \sqrt{\tfrac{1}{n}\sum_i (x_i-\mu)^2}\). Best home is §1.4 (Functions & !), which already introduces `inc = x -> x + 1`; add a worked Example there (and/or a callback note in §1.8). VOICE: do NOT call it a "lambda" or compare to Python; keep it language-neutral.

- [DIRECTIVE 2026-06-17, from Gabriel] STOP improving Module 1 (§1.1–§1.10). He is DONE with it — do not pick M1 sections for the daily improvement anymore, regardless of notebook weak spots. Aim every future run at M2+ (current focus M2, then promote/flesh out M3+ and the Appendix). M1 weak spots from his notebooks should be addressed by drilling them into LATER modules' examples, not by editing M1.
