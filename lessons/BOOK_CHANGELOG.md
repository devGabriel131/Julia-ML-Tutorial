# Book Changelog — Julia ML Competitor

One dated line per daily editor run. Newest at the bottom.

- 2026-06-11 — VISUALIZATION: added the book's first inline SVG to §1.5 Arrays — a theme-aware column-major memory-layout diagram (2×3 matrix → contiguous linear memory, color-coded by column, offsets labeled) with a worked-offset caption. Added reusable `.fig`/`.figcap` CSS for future diagrams.
- 2026-06-12 — INTEGRATION: promoted Module 2 (Linear Algebra I) from roadmap page to nine full book sections (2.1 vectors/addition/scaling, 2.2 span, 2.3 dot product, 2.4 norms, 2.5 matrices, 2.6 transpose & identity, 2.7 Xβ & matrix products, 2.8 LinearAlgebra stdlib & column-major, 2.9 practice with P1–P4, three hash-checked self-checks, and the match-the-stdlib boss), sourced from the M2 lesson notes with all Python/numpy framing stripped; wired into the chapters array and Prev/Next order.
- 2026-06-13 — EXERCISES: added a worked Example to §2.3 (Dot Product) that turns Reading 2 into concrete numbers — extracts the 45° angle between [1,0] and [1,1] and the 90° orthogonality of [3,4]·[4,-3] via acosd, with runnable LinearAlgebra code — plus a new hash-checked self-check asking for the angle between [2,0] and [3,3] (answer 45). Aimed at the geometric reading, which was prose-only.
- 2026-06-14 — EXERCISES (feedback-driven): added a code-first Worked Example to §1.3 (Multiple Dispatch) — the `collide(Ship, Asteroid)` drill where he types the four methods and watches dispatch pick on BOTH argument types, plus a nested Note showing the single-dispatch `isa`-ladder "fake" and why it doesn't scale. Targets the #1 gradebook weak spot and the vague Q1.3.2 answer in notebooks/Idiomatic.jl; RQ 1.3.2 left as-is per BOOK_FEEDBACK.
- 2026-06-16 — EXERCISES (feedback-driven, high-priority request): added a two-layer Worked Example to §1.4 (Functions & !) teaching the `->` operator via the M1 exam boss line `σ = sqrt(sum(x -> abs2(x - μ), v) / n)`. Layer 1: `->` builds an anonymous function / closure capturing μ, contrasted with the named `f(x)=abs2(x-μ)`. Layer 2: `sum(f, v)` map-reduces in one pass with zero intermediate array, contrasted with the comprehension temporary — tying back to why the fast normalizer beats the slow one (echoes his `sumsq(x)=sum(x.^2)` in notebooks/Idiomatic.jl). Variance/σ typeset in LaTeX; language-neutral, no lambda/Python framing.
- 2026-06-16 — CLARITY/LOGIC (feedback- + notebook-driven): closed the open BOOK_FEEDBACK "destructure ≠ destructive" item in §1.4 (Functions & !). Added a Note "Destructuring is not destructive" stating `lo, hi = bounds(v)` only binds names and leaves the immutable tuple and `v` untouched — the exact reverse of a mutating `!` — plus a red/green contrast card targeting his actual notebook mistake (`bounds(a)=(minimum!(a),maximum!(a))`): the trap explains `minimum!(r,A)` reduces into a preallocated `r` and mutates it (no "smallest element" meaning), the idiom gives `minimum`/`maximum` + destructuring with the `extrema` one-pass aside. RQ 1.4.2 left as-is per feedback. Extended same run with a runnable "prove it — binding vs. mutating" Worked Example: pane 1 shows `lo, hi = bounds(v)` leaving `v == [3,1,2]`; pane 2 shows the real `minimum!(r, A)` reducing each row into a preallocated `r → [1;2]` and mutating it while `A` is untouched — turning the trap card from assertion into something he can run and verify.
- 2026-06-16 — CLARITY/INTEGRATION (user-directed, multi-pass M2 LA enrichment + Strang↔Lay vocabulary): (1) §2.7 added the **column picture** of \(Ax\) — \(Ax = x_1a_{:,1}+\cdots+x_na_{:,n}\), a linear combination of A's columns — alongside the existing row picture, with runnable Julia proving `A*x`, the column combo, and the per-row dot all give `[2,7,2]`, plus a "why both pictures" note tying the column space to least-squares projection in M5 and a new column-picture RQ. (2) §2.2 added a Definition of linear independence/dependence and a "your two textbooks, reconciled" bridge table (span ↔ column space C(A) ↔ Lay's vector equation; independence ↔ full column rank). (3) §2.3 glossed dot product = inner product = \(a^\top b\) (Lay's Ch 6 term). (4) §2.6 added a "row space" Vocabulary note (C(Aᵀ), Strang's four subspaces preview). (5) M2 intro added a "spine of this module" note framing the row/column duality and the three-source vocabulary alignment. Honors THE LOCK; no Python framing; all tags/math balanced.
- 2026-06-16 — CLARITY/VISUALIZATION/LOGIC (user-directed critique fixes, six items): (1) §2.7 added a theme-aware inline SVG of the **column picture** — the parallelogram \(a_1+a_2=b\) for \(A=[3\,1;1\,2]\), b=(4,3) — second SVG in the book. (2) Built a reusable collapsible **answer toggle** (`details.ans` CSS, native `<details>`) and added worked answers to all 17 conceptual Reading-Question sets across M1 (§1.1–1.9) and M2 (§2.1–2.8) — closes the "RQs had no answer mechanism / he answered imprecisely in notebooks" gap. (3) §2.7 added a scope+solver note: M2 reads \(Ax=b\) and defers elimination/echelon/pivots/`A \ b` to M5. (4) §2.7 added the matrix–matrix **column reading** \((AB)_{:,j}=A\,b_{:,j}\). (5) §2.4 defined the **L∞ (max) norm** (with table row) where L1/L2 live, before §2.8 uses `norm(v,Inf)`. (6) Fixed a stray orphan `</p>` in the §1.3 dispatch Definition. All tags/math balanced (details 17/17, svg 2/2); nav intact; no Python framing.
- 2026-06-17 — CLARITY/EXERCISES (notebook-driven): targeted the live "most consequential type" misconception in §1.2 (Types). His pushed Idiomatic.jl Q1.2.2 says eltype returns "not the most prevailing type, but the most consequential type," and he wrote `eltype([1 2.0])`/`eltype([1 2 3])` with SPACES (the Matrix-vs-Vector literal slip the gradebook flags). Added a Worked Example teaching `eltype` as `promote_type` folded over the elements — runnable panes showing `promote_type(Int64,Float64)==Float64`, `[1,2.0]→Vector{Float64}` (1 converts up because it *fits*, not because it's "less important"), `[1,2,3]→Int64` (nothing to promote), and `[1,2.0,3//1]→Vector{Float64}` (the rule folds across Int/Float/Rational) — plus a red/green contrast card killing both errors at once: there is no "importance," and `[1 2.0]` (spaces) builds a `1×2 Matrix`, while commas `[1,2.0]` make the `Vector{Float64}` he meant. No Python framing; tags/math/nav balanced.
- 2026-06-18 — VISUALIZATION (rotating off recent CLARITY/EXERCISES runs; serves the module's spine): added the book's 3rd inline SVG to §2.2 (Linear Combinations & Span) — a theme-aware triptych in ℝ²: (1) span{v} = a line, (2) span{u,w} of two independent vectors = the whole plane (shaded "every point"), (3) span{v,2v} = still a line because 2v lies on the line v already gave. Caption ties straight to the independence/dependence Definition above it and the multicollinearity note below ("more vectors ≠ bigger span; only independent ones enlarge it"). Span/independence was the conceptual heart of M2 but prose+code only with no figure. Uses the established --julia2/--julia3/--accent/--soft/--line/--ink vars + sg/sp arrow markers; nav untouched (45/45 ids match), 3/3 svg balanced, no Python framing, math delimiters balanced.
